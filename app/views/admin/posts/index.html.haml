- title "Blog"

%ul
  - for post in @posts
    %li
      %h3= link_to post.titulo, admin_post_path(post)
      %p= post.descricao_html
      %small
        = link_to 'Editar', edit_admin_post_path(post)
        |
        = link_to 'Apagar', [:confirm_destroy, :admin,post], :class => "delete"

%p= will_paginate @posts, :previous_label => "&laquo; Anterior", :next_label => 'Próximo &raquo;'

%p= link_to "Novo Post", new_admin_post_path
